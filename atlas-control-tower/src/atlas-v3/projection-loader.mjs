export const V3_MANIFEST_RELATIVE='../data/v3/current/manifest.json';
function fail(message){throw new Error(`Atlas V3 ${message}`)}
export function validateAtlasV3Snapshot(manifest,snapshot){if(!manifest||typeof manifest!=='object')fail('manifest inválido');if(manifest.authority!=='TOWER_V06')fail('authority inválida');if(manifest.projectionOnly!==true)fail('projectionOnly inválido');if(!snapshot||typeof snapshot!=='object'||!snapshot.manifest)fail('snapshot inválido');if(snapshot.manifest.authority!=='TOWER_V06')fail('snapshot authority inválida');if(snapshot.manifest.projectionOnly!==true)fail('snapshot projection inválida');if(snapshot.manifest.fingerprint!==manifest.fingerprint)fail('fingerprint divergente');if(!Array.isArray(snapshot.graph?.root?.nodes)||!Array.isArray(snapshot.graph?.root?.edges))fail('graph inválido');return snapshot}
export function resolveAtlasV3SnapshotUrl(manifestUrl,snapshotPath){if(!snapshotPath)fail('snapshotPath ausente');const manifestBase=manifestUrl instanceof URL?manifestUrl:new URL(manifestUrl);const allowedBase=new URL('../snapshots/',manifestBase);const snapshotUrl=new URL(snapshotPath,manifestBase);if(snapshotUrl.origin!==allowedBase.origin||!snapshotUrl.pathname.startsWith(allowedBase.pathname))fail('snapshotPath fora da projeção V3');return snapshotUrl}
async function readJson(url,fetcher){const response=await fetcher(url,{cache:'no-store'});if(!response.ok)fail(`${url.pathname} HTTP ${response.status}`);return response.json()}
export async function loadAtlasV3Snapshot(baseUrl=globalThis.location?.href,fetcher=globalThis.fetch){if(!baseUrl)fail('base URL indisponível');if(typeof fetcher!=='function')fail('fetch indisponível');const manifestUrl=new URL(V3_MANIFEST_RELATIVE,baseUrl);const manifest=await readJson(manifestUrl,fetcher);if(manifest.contractVersion!=='ATLAS_PROJECTION_V3')fail('contract inválido');if(manifest.authority!=='TOWER_V06'||manifest.projectionOnly!==true)fail('manifest authority/projection inválido');const snapshotUrl=resolveAtlasV3SnapshotUrl(manifestUrl,manifest.snapshotPath);const snapshot=await readJson(snapshotUrl,fetcher);validateAtlasV3Snapshot(manifest,snapshot);return{manifest,snapshot,manifestUrl,snapshotUrl}}
