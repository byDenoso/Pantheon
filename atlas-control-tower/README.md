# NEXO Atlas Control Tower

This directory is the canonical Git-side provenance pointer for Atlas. It is **not** evidence that the currently deployed source is mirrored in this repository.

## Runtime truth

- Vercel project: `prj_DLQSz5OiIT1HxWMn2i4AgoIv5x8r`
- Production alias: `nexo-atlas-control-tower.vercel.app`
- Verified production deployment: `dpl_2mxarQaGFfozfzAZegdGC5nUbVob`
- Runtime state at verification: `READY`

## Source provenance status

`SOURCE_NOT_MIRRORED_IN_REPO`

Do not infer deploy-to-commit parity from this directory or from `main`. The exact deployment source must be imported/committed and then compared before Git may be treated as the source truth for the current Atlas deployment.

Until then:

- Vercel is authoritative for live deployment/runtime state.
- Neon `science_v1` / `learning_v1` remain the mutable scientific/learning truth owners.
- This manifest exists to prevent the old `atlas-control-tower/` pointer from resolving to a 404 while preserving the provenance gap explicitly.

Verified: 2026-09-06 23:17 BRT.
