import {normalizeAtlasEnvelope,sliceSnapshot,buildPresentStory} from './snapshot-contract.mjs';
const URLS={atlas:new URL('../../data/atlas.json',import.meta.url),lineage:new URL('../../data/lineage.json',import.meta.url),presentation:new URL('../../data/presentation.json',import.meta.url)};let cache=null;
async function readJson(url,force=false){const target=force?new URL(`${url.href}${url.search?'&':'?'}_=${Date.now()}`):url,response=await fetch(target,{cache:force?'no-store':'default'});if(!response.ok)throw new Error(`${response.status} ${url.pathname}`);return response.json()}
export async function loadSnapshotBundle({force=false}={}){if(cache&&!force)return cache;const[atlasRaw,lineage,presentation]=await Promise.all([readJson(URLS.atlas,force),readJson(URLS.lineage,force),readJson(URLS.presentation,force)]),atlas=normalizeAtlasEnvelope(atlasRaw);for(const sibling of[lineage,presentation]){if(sibling.schemaVersion!==atlas.schemaVersion)throw new Error('Snapshot schema mismatch');if(sibling.source!=='GOOGLE_DRIVE')throw new Error('Snapshot source mismatch');if(sibling.fingerprint!==atlas.fingerprint)throw new Error('Snapshot fingerprint mismatch')}cache={atlas,lineage,presentation};return cache}
export async function getGraph(options={}){const{atlas}=await loadSnapshotBundle({force:Boolean(options.force)});return sliceSnapshot(atlas,options)}
export async function getSnapshotHealth({force=false}={}){const{atlas}=await loadSnapshotBundle({force});return{ok:true,source:atlas.source,schemaVersion:atlas.schemaVersion,generatedAt:atlas.generatedAt,fingerprint:atlas.fingerprint,semanticIndex:{count:atlas.data.nodes.length},nodeCount:atlas.data.nodes.length,edgeCount:atlas.data.edges.length}}
export async function getPresentStory(focus,{force=false}={}){const{atlas,presentation}=await loadSnapshotBundle({force}),derived=buildPresentStory(atlas,focus),authored=presentation.data?.stories?.[focus]||null;return authored?{...derived,...authored,parent:derived.parent,current:derived.current,children:derived.children}:derived}
export function clearSnapshotCache(){cache=null}
