import test from 'node:test';import assert from 'node:assert/strict';import {project,layout} from '../graph3d.mjs';
test('depth changes apparent size in perspective',()=>{assert.ok(project([0,0,100],{yaw:0,pitch:0,zoom:1},800,600).scale>project([0,0,-100],{yaw:0,pitch:0,zoom:1},800,600).scale)});
test('rotation changes projected positions',()=>{assert.notEqual(project([100,0,40],{yaw:0,pitch:0,zoom:1},800,600).x,project([100,0,40],{yaw:1,pitch:0,zoom:1},800,600).x)});
test('layout positions are finite and stable for bounded graph',()=>{const n=Array.from({length:160},(_,i)=>({id:String(i),type:'TEST'}));const p=layout(n,'0');assert.equal(p.length,160);assert.ok(p.flat().every(Number.isFinite));assert.deepEqual(p,layout(n,'0'))});
test('touch gestures do not throw after one finger releases',async()=>{const {Graph3D}=await import('../graph3d.mjs');global.ResizeObserver=class{observe(){}};const listeners={};const canvas={getContext(){return{}},addEventListener(k,f){listeners[k]=f},setPointerCapture(){},style:{}};const g=new Graph3D(canvas,{select(){},open(){}});g.draw=()=>{};g.hit=()=>null;const e=(id,x)=>({pointerId:id,offsetX:x,offsetY:10,button:0});listeners.pointerdown(e(1,10));listeners.pointerdown(e(2,30));listeners.pointermove(e(2,35));listeners.pointerup(e(2,35));assert.doesNotThrow(()=>listeners.pointermove(e(1,20)))});
