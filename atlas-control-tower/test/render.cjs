const {createCanvas}=require(process.env.CODEX_PRIMARY_RUNTIME_NODE_MODULES+'/@napi-rs/canvas');const fs=require('node:fs');
(async()=>{global.devicePixelRatio=1;global.ResizeObserver=class{observe(){}};const {Graph3D}=await import('../graph3d.mjs');const handler=(await import('../api/atlas.js')).default;let data;await handler({url:'/api/graph?focus=system:SCIENCE',method:'GET',headers:{}},{setHeader(){},end(s){data=JSON.parse(s)}});const canvas=createCanvas(1150,650);canvas.clientWidth=1150;canvas.clientHeight=650;canvas.addEventListener=()=>{};canvas.style={};const graph=new Graph3D(canvas,{select(){},open(){}});graph.set(data,'system:SCIENCE');fs.writeFileSync('graph-render.png',canvas.toBuffer('image/png'));console.log('Rendered',data.nodes.length,'nodes and',data.edges.length,'edges');let p=graph.points.find(p=>p.node.id==='system:SCIENCE');if(graph.hit(p.x,p.y)?.id!=='system:SCIENCE')throw Error('Hit-test failed');graph.camera.yaw=1;graph.draw();console.log('Selection and orbit PASS')})().catch(e=>{console.error(e);process.exit(1)});
