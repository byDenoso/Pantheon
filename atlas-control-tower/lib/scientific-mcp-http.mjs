import {createHash,timingSafeEqual} from 'node:crypto';
import {DEFAULT_RUNNER_KEY_SHA256} from './durable-runner.mjs';
import {handleMcpRpc,isPublicDiscoveryTool,toolDescriptors} from './scientific-mcp.mjs';

function sha256(value){return createHash('sha256').update(String(value)).digest('hex')}
function safeEqualHex(a,b){if(!/^[0-9a-f]{64}$/i.test(String(a||''))||!/^[0-9a-f]{64}$/i.test(String(b||'')))return false;const left=Buffer.from(String(a).toLowerCase(),'hex'),right=Buffer.from(String(b).toLowerCase(),'hex');return left.length===right.length&&timingSafeEqual(left,right)}
function bearer(headers={}){const raw=headers.authorization||headers.Authorization||'';const match=String(raw).match(/^Bearer\s+(.+)$/i);return match?.[1]?.trim()||''}
function expectedAccessHash(env){return env?.NEXO_MCP_ACCESS_KEY_SHA256||env?.NEXO_RUNNER_KEY_SHA256||DEFAULT_RUNNER_KEY_SHA256}
function authorized(headers,env){const value=bearer(headers);return Boolean(value)&&safeEqualHex(sha256(value),expectedAccessHash(env))}
function parseBody(body){if(body&&typeof body==='object'&&!Buffer.isBuffer(body))return body;if(typeof body==='string'&&body.trim())return JSON.parse(body);if(Buffer.isBuffer(body)&&body.length)return JSON.parse(body.toString('utf8'));return null}
function send(res,status,payload){res.statusCode=status;res.setHeader?.('Content-Type','application/json; charset=utf-8');res.setHeader?.('Cache-Control','no-store');if(typeof res.status==='function'&&typeof res.json==='function')return res.status(status).json(payload);if(typeof res.end==='function')return res.end(JSON.stringify(payload));return payload}
export function createScientificMcpHttpHandler({env=process.env,gateway,service}){if(!gateway||!service)throw new Error('MCP_HTTP_DEPENDENCIES_REQUIRED');return async function scientificMcpHttp(req,res){const method=String(req?.method||'GET').toUpperCase();if(method==='OPTIONS'){res.statusCode=204;res.setHeader?.('Allow','GET, POST, OPTIONS');return res.end?.('')}if(method==='GET'){const bootstrap=await service.getBootstrap();return send(res,200,{ok:true,server:'nexo-capability-bootstrap',version:'1.1.0',authority:'TOWER_V06',tools:toolDescriptors().map(item=>item.name),bootstrap,towerWriteConfigured:Boolean(gateway.configured?.towerWrite)})}if(method!=='POST')return send(res,405,{error:'METHOD_NOT_ALLOWED'});let message;try{message=parseBody(req?.body)}catch{return send(res,400,{jsonrpc:'2.0',id:null,error:{code:-32700,message:'Parse error'}})}if(!message)return send(res,400,{jsonrpc:'2.0',id:null,error:{code:-32600,message:'Invalid Request'}});const calledTool=message?.method==='tools/call'?message?.params?.name:null;if(message.method==='tools/call'&&!isPublicDiscoveryTool(calledTool)&&!authorized(req?.headers||{},env))return send(res,401,{jsonrpc:'2.0',id:message.id??null,error:{code:-32001,message:'Unauthorized'}});const response=await handleMcpRpc(message,{service});if(response===null){res.statusCode=204;return res.end?.('')}return send(res,200,response)}}
export const _internal={sha256,bearer,authorized,expectedAccessHash,safeEqualHex};
