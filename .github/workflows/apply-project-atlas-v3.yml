name: Apply Project Atlas v3

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Migration disabled: verified payload was not fully materialized.'
          echo 'Use the complete verified repository package and a dedicated repository.'
