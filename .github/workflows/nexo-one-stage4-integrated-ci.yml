name: NEXO ONE Stage 4 Integrated CI

on:
  push:
    branches:
      - chatgpt/nexo-one-stage4-integrated
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: nexo-one/package-lock.json
      - name: Install
        working-directory: nexo-one
        run: npm ci --no-audit --no-fund
      - name: Typecheck
        working-directory: nexo-one
        run: npm run typecheck
      - name: Focused Stage 1-4 tests
        working-directory: nexo-one
        run: node --test test/galaxy-compiler.test.mjs test/galaxy-snapshot-pipeline.test.mjs test/galaxy-snapshot-loader.test.mjs test/galaxy-hardening.test.mjs test/galaxy-stage2.test.mjs test/graph3d.test.mjs test/atlas-stage3-product.test.mjs test/galaxy-deep-link.test.mjs test/galaxy-layers.test.mjs test/galaxy-tour.test.mjs test/galaxy-webmcp.test.mjs test/mobile-regressions.test.mjs
      - name: Full regression suite
        working-directory: nexo-one
        run: npm test
      - name: Style lint
        working-directory: nexo-one
        run: npm run style:lint
      - name: Production build
        working-directory: nexo-one
        run: npm run build
